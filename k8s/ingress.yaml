apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-flask-app
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simple-flask-app
                port:
                  number: 80
